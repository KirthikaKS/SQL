SELECT*
FROM EmployeeDemographics

SELECT FirstName, Gender FROM EmployeeDemographics

SELECT TOP 6 *
FROM EmployeeSalary

SELECT DISTINCT(EmployeeID)
FROM EmployeeDemographics

SELECT DISTINCT (Salary)
FROM EmployeeSalary

SELECT MAX(Salary)
FROM EmployeeSalary

SELECT MIN(Salary)
FROM EmployeeSalary

SELECT AVG(SALARY)
FROM EmployeeSalary

